# Level 6 — Production Incidents (planned)

Not built yet. These combine everything from Levels 1-5 into a single
synthetic incident — logs, metrics, and a broken environment, no hints,
find the root cause. Example framing:

> Incident #12: Latency increased from 8ms to 1.2s. Error rate is 12%.
> CPU is only 25%. Memory looks normal. Customers cannot log in.
> Find the root cause.

Will follow the same per-lab format as [Level 1](../linux) and
[Level 2](../networking): `README.md`, `solution.md`, `CONCEPTS.md`,
`setup.sh`, `check.sh`, `reset.sh`.
