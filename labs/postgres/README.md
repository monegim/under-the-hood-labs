# Level 4 — Databases: PostgreSQL (planned)

Not built yet. Planned topics: replication lag, WAL full, autovacuum
disabled, index bloat, lock contention.

See [`labs/mysql/`](../mysql) for the MySQL half of Level 4, already
seeded with one lab. Will follow the same per-lab format as
[Level 1](../linux) and [Level 2](../networking): `README.md`,
`solution.md`, `CONCEPTS.md`, `setup.sh`, `check.sh`, `reset.sh`.
