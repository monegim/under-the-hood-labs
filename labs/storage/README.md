# Level 3 — Storage (planned)

Not built yet. Planned topics: LVM full, XFS corruption, ext4 recovery,
RAID degraded, slow/failing disks (including NVMe), read-only
filesystems, inode exhaustion.

Will follow the same per-lab format as [Level 1](../linux) and
[Level 2](../networking): `README.md`, `solution.md`, `CONCEPTS.md`,
`setup.sh`, `check.sh`, `reset.sh`.
